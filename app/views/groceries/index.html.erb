<% if @groceries.blank? %>
  <h5> Start adding items to your grocery </h5>
<% end %>

<ul class="collection">
  <% @groceries.each do |grocery| %>
    <li class="collection-item">
      <%= grocery.quantity %> - <%= grocery.name %>
      <%= link_to grocery_path(grocery), method: :delete, data: { confirm: 'Remove item from grocery list?' }, class: 'right' do %>
        <i class="material-icons">delete</i>
      <% end %>
    </li>
  <% end %>
</ul>

<% if @new_button %>
  <%= link_to ("<i class=\"material-icons\">add</i>").html_safe, new_grocery_path, class: "btn-floating waves-effect waves-light right-align right blue" %>
<% else %>
  <%= link_to 'Add item', new_grocery_path, class: "waves-effect waves-light btn white-text right blue" %>
<% end %>
